规范：
1. 写好注释很重要
2. 做完一个小功能，应该及时提交代码，一天至少提交一次代码
3. 谨防intent/bundle传递大量数据的错误
4. 不要使用Eventbus和ButterKnife，如有特殊场景，可一起商量
5. 不要在布局文件的标题抽取到string.xml文件，对于内容频繁更新的view，使用tools:tooltipText查看预览情况
6. 新增一个库，记得添加对应的混淆规则

MVP更适合大型项目，可以直接回调

已解决的一些细节：
1. 公共类库版本统一管理
2. 签名打包，不同版本不同URL
3. 兼容7.0以上文件系统访问的坑
4. 兼容9.0以上版本无法访问http服务器的问题
5. debug模式打印日志，release模式不打日志，兼容特殊要求，如打印行号
6. 网络请求详情在通知栏显示
7. 动态权限申请
8. 解决内存泄露避免和检测
9. 解决网络（耗时）任务分类（可见、黏性可见，不可见）可以自动回收，返回的类型可以有基类包裹或者自定义类
10. ViewBinding
11. Presenter延迟加载


一些推荐的库：
1.手机自建服务器：
```
implementation "io.ktor:ktor-server-core:2.3.3"
implementation "io.ktor:ktor-server-netty:2.3.3"
```
2.扫码：
华为扫描

3.动画：
Lottie

待定：
集成崩溃统计
热更新（增量更新）
下拉刷新库
